# Backend URL — Vite dev server proxies /api/* to this target.
VITE_API_PROXY_TARGET=http://localhost:8000

# When true, the frontend uses an in-memory mock instead of calling the backend.
# Flip to false once your REST API is reachable.
VITE_USE_MOCK=true
